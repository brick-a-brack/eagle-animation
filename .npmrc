# react-keyboard-event-handler still declares a peer dep on react 15/16 while the
# app runs on react 19. legacy-peer-deps keeps `npm install`/`npm ci` working
# consistently across Windows, macOS and Linux without needing the CLI flag.
legacy-peer-deps=true
