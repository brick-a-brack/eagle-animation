import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/*
  getProjectsList(DEFAULT_PATH).then((e) => {
    console.log('DIR', e);
  }).catch((e) => {
    console.log('DIR', e);
  });
*/

ReactDOM.render(
  <App />, document.getElementById('root'),
);
